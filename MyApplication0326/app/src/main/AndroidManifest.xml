<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.org.iii.myapplication0325">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyCard" />
        <activity android:name=".SystemSetting" />
        <activity android:name=".CardListGroup" />
        <activity android:name=".ActListGroup" />
        <activity android:name=".ActMap" />
        <activity android:name=".ActCreate" />
        <activity android:name=".QrCode" />
        <activity android:name=".ActMessageBoard" />
        <activity android:name=".CardCreate" />
        <activity android:name=".CardDetial"></activity>
    </application>

</manifest>